=============
The Dashboard
=============

The dashboard has been built using the Django web framework for python.
It simple reads the dataset_details.json file generated by dashboard_script.py and shows it on the frontend.

